// instead of hardcoding event names, having it as a configurable option
// allows us to maintain them easily and also reduces the risk of bugs because of typos
module.exports = {
    // we can define more such events
    events: {
        GREET: 'greet'
    }
}