// Immediately invoked function expressions (IIFEs)
// objects that can be immediately invoked

// here we are creating a function and invoking immediately, note the () at the end
(function (text) {
    console.log(text);
}('This is an IIFE'));
